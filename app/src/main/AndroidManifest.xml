<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="startup.app.speedopt">

    <application
        android:name=".application.StartUpApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AppStartUpMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<!--
 * StartUpApplication
    adb shell am start -S -W com.yzl.yuanzi/com.yzl.yuanzi.GuideActivity

    * adb shell am start -S -W  startup.app.speedopt/startup.app.speedopt.AppStartUpMainActivity

-->
